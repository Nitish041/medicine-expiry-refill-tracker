const express = require('express');
const router = express.Router();

// Placeholder sales routes to satisfy server imports.
// Implement full sales logic later as needed.

router.get('/', async (req, res) => {
  res.json([]);
});

module.exports = router;
